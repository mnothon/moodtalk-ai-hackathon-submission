<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="03-add-assignments-table" author="jop" objectQuotingStrategy="QUOTE_ALL_OBJECTS">
        <createTable tableName="assignments">
            <column name="id" type="VARCHAR(36)">
                <constraints primaryKey="true" primaryKeyName="pk_assignments"/>
            </column>

            <column name="employee_id" type="VARCHAR(36)">
                <constraints nullable="false"/>
            </column>

            <column name="project_id" type="VARCHAR(36)">
                <constraints nullable="false"/>
            </column>

            <column name="date" type="DATE">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addForeignKeyConstraint
                baseTableName="assignments"
                baseColumnNames="employee_id"
                referencedTableName="employee"
                referencedColumnNames="id"
                constraintName="fk_assignments_employee"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="assignments"
                baseColumnNames="project_id"
                referencedTableName="project"
                referencedColumnNames="id"
                constraintName="fk_assignments_project"
                onDelete="RESTRICT"/>
        <addUniqueConstraint
                tableName="assignments"
                columnNames="employee_id, date"
                constraintName="uq_assignments_employee_date"/>
    </changeSet>
</databaseChangeLog>